---
const { title, date, image, description } = Astro.props;
---

<div class="max-w-md bg-white rounded-lg shadow-md overflow-hidden">
  <img src={image} alt={title} class="w-full h-48 object-cover" />
  <div class="p-4">
    <p class="text-gray-600 text-sm">{date}</p>
    <h3 class="text-lg font-semibold text-gray-800">{title}</h3>
    <p class="text-gray-600 text-sm mt-2 line-clamp-2">{description}</p>
    <a href="#" class="mt-3 inline-block text-blue-500 font-medium"
      >Дэлгэрэнгүй →</a
    >
  </div>
</div>
