---
import "../styles/global.css";
import { twMerge } from "tailwind-merge";
---

<script defer src="https://cdn.jsdelivr.net/npm/alpinejs@3.x.x/dist/cdn.min.js"
></script>
<div
  class="bg-blue-900 flex items-center justify-between px-5 py-7 fixed top-0 left-0 w-full z-50 shadow-md"
>
  <!-- Лого ба нэр (Голлуулсан) -->
  <div class="flex-1 flex justify-start items-center space-x-4">
    <img
      src="/public/image/image.png"
      alt="NUM Logo"
      class="px-5 h-12 w-auto"
    />
    <h1 class="text-lg font-medium text-white">МОНГОЛ УЛСЫН ИХ СУРГУУЛЬ</h1>
  </div>

  <div class="flex items-center space-x-4">
    <!-- Хэл сонголт -->
    <div x-data="{ open: false }" class="relative">
      <button @click="open = !open" class="focus:outline-none">
        <img
          src="/public/image/world.png"
          alt="globe icon"
          class="h-8 w-auto"
        />
      </button>
      <div
        x-show="open"
        @click.away="open = false"
        class="absolute right-0 mt-2 w-40 bg-white text-black rounded-lg shadow-lg overflow-hidden"
      >
        <a href="#" class="block px-4 py-2 hover:bg-gray-200">Монгол</a>
        <a href="#" class="block px-4 py-2 hover:bg-gray-200">English</a>
        <a href="#" class="block px-4 py-2 hover:bg-gray-200">Русский</a>
      </div>
    </div>

    <!-- Цэс (Menu) -->
    <div x-data="{ open: false }" class="relative px-3">
      <button @click="open = !open" class="focus:outline-none">
        <img src="/public/image/menu.png" alt="menu icon" class="h-7 w-auto" />
      </button>
      <div
        x-show="open"
        @click.away="open = false"
        class="absolute right-0 mt-2 w-48 bg-white text-black rounded-lg shadow-lg overflow-hidden"
      >
        <a href="#" class="block px-4 py-2 hover:bg-gray-200">🏠 Нүүр</a>
        <a href="#" class="block px-4 py-2 hover:bg-gray-200">📜 Мэдээ</a>
        <a href="#" class="block px-4 py-2 hover:bg-gray-200">📞 Холбоо барих</a
        >
      </div>
    </div>
  </div>
</div>
